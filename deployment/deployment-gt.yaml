apiVersion: apps/v1
kind: Deployment
metadata:
  name: sergio-demo-gt
  labels:
    app: sergio-demo-gt
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sergio-demo-gt
  template:
    metadata:
      labels:
        app: sergio-demo-gt
    spec:
      containers:
        - name: sergio-demo-gt
          image: crmultitenant.azurecr.io/sergio/demo:1.0.0
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: sergio-demo-gt
spec:
  selector:
    app: sergio-demo-gt
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP